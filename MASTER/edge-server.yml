server:
    port: 8765
 
eureka:
    client:
        serviceUrl:
             defaultZone: http://localhost:8761/eureka/

zuul:
    debug:
        request: true
    routes:
        authorization-manager-server:
              path: /authorization-manager-server/user/**
              serviceId: authorization-manager-server
              stripPrefix: true